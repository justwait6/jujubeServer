// ErrorCode, in consistent with client
let ErrorCode = new Array();

ErrorCode.TOKEN_EXPIRED = -10001;
ErrorCode.PARAM_INVALID = -10002;
ErrorCode.PARAM_FORMAT = -10003;

module.exports = ErrorCode;
